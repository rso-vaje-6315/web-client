import { RsoWebClientEnv } from "./env.model";

export const environment: RsoWebClientEnv = {
    production: true,
    apiUrl: "",
    keycloak: {
        clientId: "",
        realm: "",
        url: ""
    }
};
